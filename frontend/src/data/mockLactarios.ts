// Unified mock data — references MOCK_LACTARIOS from constants.ts
// Used only as fallback during development when the API is unavailable.
export { MOCK_LACTARIOS as mockLactarios } from '../constants';
